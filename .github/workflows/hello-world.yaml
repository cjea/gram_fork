name: CLI Test

on:
  pull_request:
    branches:
      - main
      - cli

jobs:
  hello:
    name: Iterate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"

      - name: "[Simulate] Install CLI"
        run: |
          cd server
          go build -o gram_cli ./cli/main.go
          stat gram_cli
          ls -lh gram_cli

      - name: Run CLI
        env:
          GRAM_API_KEY: ${{ secrets.GRAM_API_KEY }}
        run: cd server && ./gram_cli
